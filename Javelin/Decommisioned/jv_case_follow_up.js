/**
 * @author JoeSon 
 * Scheduled script which brings out all cases with Status of "Customer to reply"
 * Retrieve records with following columns:
 * - Internal ID
 * - Waiting Reply Set Date
 */

var lastProcessed='';

function autoResponseToCase() {
	
}
