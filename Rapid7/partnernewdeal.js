function NewDeal (type, fld)
{
//if ( type == 'create' ) 
//{
var currentuser = nlapiGetUser();
//var currentuser = nlobjContext.getUser = function(); 
nlapiSetFieldValue('partner',currentuser);
//}
{	return confirm("Your deal registration will be processed within 1 business day.");}
}
